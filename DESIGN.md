# Article Translator Design System

## 1. 视觉主题与氛围

像在原文章旁放置一本排版克制的中文技术读本：温暖、安静、可信。原网页是主角，译读以 Chrome 右侧边栏为主体，不重建原站；用户主动划词时可在原文附近出现小型操作菜单和结果浮窗。

## 2. 色板与角色

- 边栏纸张：`#f7f4ed` / `#fbfaf6`
- 主文字：`#24211d`
- 次文字：`#6f695f`
- 品牌强调：`#a94f32`
- 术语高亮：`#ead8a8`
- 成功状态：`#4f765d`
- 边界：`#ded8cb`

## 3. 排版规则

- 中文正文使用系统中文字体栈，字号不小于 `16px`，行高不低于 `1.75`。
- 原文 `H1–H6` 映射为清晰递减的中文标题层级；中文只使用 `400/500/600` 字重。
- 原文加粗、下划线、链接和行内代码在译文对应位置保留；英文斜体映射为中文中等字重强调，不使用中文伪斜体。
- 数字与进度使用等宽数字。

## 4. 组件样式

- 顶栏与 Chrome Side Panel 融为一体，不重复浏览器自带关闭控件。
- 即时布尔选项使用紧凑 switch；AI 专有名词高亮每次打开默认关闭。
- 状态文字只在读取、翻译和错误时出现，完成状态不常驻抢占版面。
- 标题、正文、列表、引用分别使用语义化元素；引用使用整块浅色背景和全边框，不使用左侧竖线。
- 翻译按边栏视区渐进触发；未进入阅读范围的内容显示匹配尺寸的骨架，缓存命中时即时恢复。

## 5. 布局原则

- 原网页保持原样，图片、图表、视频和交互模块继续留在原网页中。
- 右侧边栏按原文顺序展示可线性阅读的中文文章；表格和多列复杂模块仅显示跳过提示，避免打散内部结构；原文滚动单向带动译文滚动，但不显示当前位置、段落高亮或已读状态。
- 边栏宽度变化时内容自然重排，不追求与原文逐行等高。

## 6. 深度层级

- 页面背景为纸张 Level 0。
- 顶栏使用单条边界形成 Level 1，不使用重阴影或玻璃拟态。
- 错误和引用通过整块底色、全边框与圆角形成有限层级。
- 划词菜单贴近选择区域；结果浮窗使用暖白底、全边框和轻阴影，加载、失败、设置入口与关闭均在同一浮窗内。

## 7. Do / Don't

- Do：保留原网页、翻译线性可提取文章文本、按语义映射格式、对模型输出做白名单渲染。
- Don't：不 OCR 图片文字，不复制交互模块，不使用当前位置高亮、已读状态、紫蓝渐变、纯黑、斜体、左侧竖线或卡片堆叠。

## 8. 响应式

- 支持 Chrome Side Panel 的可调宽度，最窄 `260px` 仍可完成阅读和设置入口操作。
- `330px` 以下缩减水平留白和标题字号，控制项允许换行但不遮挡。
- 所有主要按钮最小触控高度 `36px`。

## 9. Motion 哲学

- 高频阅读操作保持即时，不添加装饰动画。
- Switch 和状态透明度反馈控制在 `180ms` 内，只动画 `transform` 与 `opacity`。
- 所有动画支持 `prefers-reduced-motion`。
