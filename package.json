{
  "name": "article-translator-demo",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check background.js && node --check content.js && node --check sidepanel.js && node --check options.js && node --check lib/translation.js && node --check lib/cache.js && node --check lib/selection.js"
  }
}
