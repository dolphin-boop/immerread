{
  "name": "yidu",
  "version": "0.5.0",
  "description": "Chrome side-panel reader for progressive Chinese translation of English technical articles.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check background.js && node --check content.js && node --check sidepanel.js && node --check options.js && node --check lib/translation.js && node --check lib/cache.js && node --check lib/glossary.js && node --check lib/selection.js && node --check lib/summary.js"
  }
}
